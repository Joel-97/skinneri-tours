/**
 * ==========================================================
 * ACCESS REQUEST STATUS
 * ==========================================================
 */

export const ACCESS_REQUEST_STATUS = Object.freeze({

    PENDING: "pending",

    APPROVED: "approved",

    REJECTED: "rejected"

});

export default ACCESS_REQUEST_STATUS;